# config/eval/policy/conditional_diffusion_simple.yaml

defaults:
  - simple
  - _self_

infer_model: conditional_diffusion

inference:
  model             : conditional_diffusion
  guidance_scale    : 0.2
  sampling_timesteps: 20
