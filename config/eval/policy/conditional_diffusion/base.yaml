# config/eval/policy/conditional_diffusion/base.yaml
# @package eval.policy

infer_model: conditional_diffusion

inference:
  model             : conditional_diffusion
  guidance_scale    : 0.2
  sampling_timesteps: 20
