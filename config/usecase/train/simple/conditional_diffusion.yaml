# config/usecase/train/simple/conditional_diffusion.yaml
# @package usecase

defaults:
  - /inferencer/conditional_diffusion@inferencer: simple
  - /dataset    : simple_2d
  - /log        : train
  - /path       : local
  - _self_

name  : train
device: cuda:0
