# config/inferencer/i2sb/complex.yaml
# @package inferencer

defaults:
  - base
  - /inferencer/network@network: dit_D128
  - _self_

trainer:
  train_batch_size         : 18
  gradient_accumulate_every: 5
